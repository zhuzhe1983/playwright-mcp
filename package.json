{
  "name": "playwright-mcp",
  "version": "1.1.0",
  "description": "MCP server for Playwright browser automation in headless mode",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "test": "npm run build && node dist/test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "playwright": "^1.40.0",
    "playwright-chromium": "^1.40.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "electron": "^37.3.1",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
